spring:
  application:
    name: exchange-api

  datasource:
    url: jdbc:mysql://localhost:3306/currencies
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: przemekzywko@gmail.com
    password: NNNN
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
    from: przemekzywko@gmail.com

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

rabbit:
  confirmationQueueName: confirmation_queue

server:
  port: 8083